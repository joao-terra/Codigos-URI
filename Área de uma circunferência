#include <stdio.h>
#define pi 3.14159
int main(void) {
 
   float raio;
   double area;
   printf ("Informe o valor do raio: ");
   scanf ("%f" , &raio);
   area = pi * raio * raio;
   printf ("A= %.4lf\n" , area);
 
    return 0;
}
